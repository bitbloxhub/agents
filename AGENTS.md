# Repo Instructions

Repo scope: **skills only**.
Treat this repository as a mostly agent-agnostic skill library.

## Structure

- `skills/`
  - `flake-aspects/`
  - `flake-file/`
  - `import-tree/`

## Rules

- Prefer editing/maintaining skill docs and assets only
- Do not reintroduce setup/bootstrap scripts in this repo
- Keep instructions focused on skill behavior and usage
- Keep skills agent-agnostic by default; document exceptions clearly
- Follow project/user formatting preferences when touching files
